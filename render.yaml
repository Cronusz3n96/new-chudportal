services:
  - type: web
    name: chudoogle-scramjet
    runtime: node
    plan: free
    rootDir: scramjet-proxy
    buildCommand: npm ci --ignore-scripts
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SELF_HOSTED
        value: "1"